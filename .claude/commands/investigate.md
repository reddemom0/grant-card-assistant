---
description: Launch read-only codebase investigation via the researcher subagent
---

Use the `researcher` subagent to investigate: $ARGUMENTS

The researcher is read-only and runs in a separate context window. When it reports back, summarize the findings in plain language and recommend next steps (but don't start implementing yet).
